{
  "name": "elementalist",
  "scripts": {
    "build": "babel -d ./build ./src -s",
    "test": "ava"
  },
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "cross-spawn-promise": "^0.10.1"
  },
  "devDependencies": {
    "ava": "^0.23.0",
    "babel-cli": "^6.26.0",
    "babel-register": "^6.26.0"
  },
  "ava": {
    "require": [
      "babel-register"
    ],
    "babel": "inherit"
  }
}
